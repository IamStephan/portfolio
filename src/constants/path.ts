import path from 'path'

const ROOT_DIR = process.cwd()
const CASE_STUDIES_DIR = path.resolve(ROOT_DIR, './content')

const CONTENT_DIR_PREFIX = 'case-studies'

export { ROOT_DIR, CASE_STUDIES_DIR, CONTENT_DIR_PREFIX }
